@model int

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Management</h4>
<hr />
<div class="row">

        <form asp-action="Edit">
        <div>
            <h5 class="rounded text-center text-white p-2 bg-info">System Configuration</</h5>
        </div>
            <div class=" = row">

                <div class="col-4 mx-2 my-2">
                    <label class="control-label"> Limitation Days</label>
                    <input name="limitationDays" class="form-control" value="@Model" />
                </div>

                <div class="form-group">
                    <input type="submit" value="Save" class="btn btn-primary my-2 mx-2" />
                </div>
            </div>
        </form>
    
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
